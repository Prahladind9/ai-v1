## Spring AI

### Defaults
- Prompt: like system 
- Advisors: like logging or custom filters that apply to each interaction
- Tools: like registers tools/functions available to the LLM by default
- Options: allows to set default configuration options for the model request like temperature, maxTokens
- User: sets a default user message that will be included in every prompt unless overridden